apply plugin: "war"

bootWar {
    mainClassName = "com.sendilkumarn.sample.kinvoice.KinvoiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
